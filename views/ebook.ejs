<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Download eBooks - Digital Library</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f2f2f2;
    }
    .container {
        width: 80%;
        margin: 20px auto;
        padding: 20px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .header {
        color: #45a049;
        font-size: 24px;
        margin-bottom: 20px;
        text-align: center;
    }
    .search-container {
        margin-bottom: 20px;
    }
    .search-container input[type=text] {
        padding: 10px;
        width: 100%;
        border-radius: 5px;
        border: 1px solid #ddd;
        box-sizing: border-box;
    }
    .book-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        grid-gap: 20px;
    }
    .book {
        background-color: #f9f9f9;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        padding: 20px;
        text-align: center;
    }
    .book-title {
        font-weight: bold;
        margin-bottom: 10px;
    }
    .book-author {
        color: #666;
    }
    .book-download {
        margin-top: 10px;
    }
    .book-download a {
        background-color: #45a049;
        color: #fff;
        text-decoration: none;
        padding: 5px 10px;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }
    .book-download a:hover {
        background-color: #357e3d;
    }
</style>
</head>
<body>
<div class="container">
    <div class="header">Download eBooks</div>
    <div class="search-container">
        <input type="text" id="search" placeholder="Search for eBooks..">
    </div>
    <div class="book-list">
        <div class="book">
    <% allEBooks.forEach(eBook => { %>
        <div class="book">
            <div class="book-title"><%=eBook.title  %></div>
            <div class="book-author">Author: <%=eBook.author  %></div>
            <div class="book-download"><a href="/dashboard/ebook/<%=eBook.isbn%>">Download</a></div>
        </div>
    <% }) %>


    </div>
</div>

<script>
    document.getElementById("search").addEventListener("keyup", function() {
        let searchValue = this.value.toLowerCase();
        let books = document.querySelectorAll(".book");
        
        books.forEach(function(book) {
            let title = book.querySelector(".book-title").textContent.toLowerCase();
            let author = book.querySelector(".book-author").textContent.toLowerCase();

            if (title.indexOf(searchValue) > -1 || author.indexOf(searchValue) > -1) {
                book.style.display = "";
            } else {
                book.style.display = "none";
            }
        });
    });
</script>
</body>
</html>

